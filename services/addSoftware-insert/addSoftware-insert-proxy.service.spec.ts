/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AddSoftwareInsertProxyService } from './addSoftware-insert-proxy.service';

describe('Service: AddSoftwareInsertProxy', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AddSoftwareInsertProxyService]
    });
  });

  it('should ...', inject([AddSoftwareInsertProxyService], (service: AddSoftwareInsertProxyService) => {
    expect(service).toBeTruthy();
  }));
});

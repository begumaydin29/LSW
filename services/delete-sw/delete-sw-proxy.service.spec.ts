/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DeleteSwProxyService } from './delete-sw-proxy.service';

describe('Service: DeleteSwProxy', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DeleteSwProxyService]
    });
  });

  it('should ...', inject([DeleteSwProxyService], (service: DeleteSwProxyService) => {
    expect(service).toBeTruthy();
  }));
});

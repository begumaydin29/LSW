/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EditSwProxyService } from './edit-sw-proxy.service';

describe('Service: EditSwProxy', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EditSwProxyService]
    });
  });

  it('should ...', inject([EditSwProxyService], (service: EditSwProxyService) => {
    expect(service).toBeTruthy();
  }));
});

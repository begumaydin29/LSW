/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AddHardwareInsertProxyService } from './addHardware-insert-proxy.service';

describe('Service: AddHardwareInsertProxy', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AddHardwareInsertProxyService]
    });
  });

  it('should ...', inject([AddHardwareInsertProxyService], (service: AddHardwareInsertProxyService) => {
    expect(service).toBeTruthy();
  }));
});

/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DeleteEoProxyService } from './delete-eo-proxy.service';

describe('Service: DeleteEoProxy', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DeleteEoProxyService]
    });
  });

  it('should ...', inject([DeleteEoProxyService], (service: DeleteEoProxyService) => {
    expect(service).toBeTruthy();
  }));
});

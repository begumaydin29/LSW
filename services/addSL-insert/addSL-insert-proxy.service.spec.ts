/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AddSLInsertProxyService } from './addSL-insert-proxy.service';

describe('Service: AddSoftwareInsertProxy', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AddSLInsertProxyService]
    });
  });

  it('should ...', inject([AddSLInsertProxyService], (service: AddSLInsertProxyService) => {
    expect(service).toBeTruthy();
  }));
});
